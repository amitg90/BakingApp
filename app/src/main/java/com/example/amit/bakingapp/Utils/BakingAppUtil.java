package com.example.amit.bakingapp.Utils;

import android.content.Context;
import android.content.SharedPreferences;
import android.util.Log;

import com.example.amit.bakingapp.MainActivity;
import com.google.gson.Gson;

public class BakingAppUtil {
    public static void store_recipe_ingredients_in_shared_memory(Context context, int recipe_id) {
//        SharedPreferences sharedPreferences = context.getSharedPreferences(SHARED_FILE, MODE_PRIVATE);
//        SharedPreferences.Editor editor = sharedPreferences.edit();
//        Gson gson = new Gson();
//
//        for (int i = 0; i< recipeInfo.ingredients.size();i++) {
//            list.add(recipeInfo.ingredients.get(i).ingredient);
//        }
//
//        // This can be any object. Does not have to be an arraylist.
//        String json = gson.toJson(list);
//
//        editor.putString(MainActivity.RECIPE_INGREDIENT_STR, json);
//        editor.apply();
//
//        Log.e("RecipeWidgetFactory", "!!Trigger Updating Widgets:");

    }
}
